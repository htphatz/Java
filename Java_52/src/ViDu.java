/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */

/**
 *
 * @author HP
 */
public class ViDu {
    public static void main(String[] args) {
        String s1 = "TITV";
        String s2 = ".VN";
        
        // Hàm concat() -> nối các chuỗi với nhau (chuỗi)
        String s3 = s1.concat(s2);
        System.out.println(s3);
        
        // Hàm replace() -> thay thế chuỗi ở vị trí mong muốn
        
        // Hàm toLowerCase() -> chuyển chuối về viết thường (chuỗi)
        // Hàm toUpperCase() -> chuyển chuối về viết in hoa (chuỗi)
        
        // Hàm trim() -> xóa khoảng trắng 2 đầu chuỗi
        
        // Hàm subString() -> cắt chuỗi hiện tại thành chuỗi con (chuỗi)
    }
}
